apiVersion: v1
kind: ConfigMap
metadata:
  name: postgres-config
  labels:
    db: kytokdb-postgres
data:
  init.sql: |
    CREATE DATABASE vinyldb_catalog;
    CREATE DATABASE vinyldb_order;

---

apiVersion: apps/v1
kind: Deployment
metadata:
  name: kytokdb-postgres
  labels:
    db: kytokdb-postgres
spec:
  selector:
    matchLabels:
      db: kytokdb-postgres
  template:
    metadata:
      labels:
        db: kytokdb-postgres
    spec:
      containers:
        - name: kytokdb-postgres
          image: postgres:14.4
          env:
            - name: POSTGRES_USER
              value: user
            - name: POSTGRES_PASSWORD
              value: password
          resources:
            requests:
              cpu: 100m
              memory: 60Mi
            limits:
              cpu: 200m
              memory: 120Mi
          volumeMounts:
            - mountPath: /docker-entrypoint-initdb.d
              name: postgres-config-volume
      volumes:
        - name: postgres-config-volume
          configMap:
            name: postgres-config

---

apiVersion: v1
kind: Service
metadata:
  name: kytokdb-postgres
  labels:
    db: kytokdb-postgres
spec:
  type: ClusterIP
  selector:
    db: kytokdb-postgres
  ports:
    - protocol: TCP
      port: 5432
      targetPort: 5432
